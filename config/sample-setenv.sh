
# secret aws credential key ID (accessKeyId)
export AWS_QCLIENT_KEYID = 'my aws accessKeyId here'

# secret access key(secretAccessKey)
export AWS_QCLIENT_SECRET ='my aws secretAccessKey'

# aws region 
export AWS_QCLIENT_REGION = 'eu-west-2'

# use this to override the AWS SWS endpoint (e.g. for using localstack = "http://localhost:4575 )
export AWS_QCLIENT_SQS_ENDPOINT 

# use this to override the AWS SQS endpoint (e.g. for using localstack = "http://localhost:4576" )
export AWS_QCLIENT_SNS_ENDPOINT


